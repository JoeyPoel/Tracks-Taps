import TermsScreen from '@/src/screens/TermsScreen';
import React from 'react';

export default function AuthTermsRoute() {
    // Reusing the TermsScreen component which is generally route-agnostic
    return <TermsScreen />;
}
